# @visx/* declares peer react ^16-18 but works fine on React 19 (pure
# SVG rendering). Without this, npm install fails with ERESOLVE on
# Vercel. Remove once visx publishes React 19 peer ranges.
legacy-peer-deps=true
